package eu.riscoss.reasoner;

/**
 * Which information about a model is being retrieved/passed back?
 * Currently, only INPUT_DATA and OUTPUT_DATA are used
 * 
 * @author albertosiena
 *
 */
public enum ModelSlice {
	INPUT_DATA, 
	OUTPUT_DATA, 
	CUSTOM
}
